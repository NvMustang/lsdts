{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/i/:inviteId", "destination": "/api/invite-page?inviteId=:inviteId" }
  ]
}

